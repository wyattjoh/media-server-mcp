# Media Server MCP Configuration
# Copy this file to .env and update with your actual values

# Radarr Configuration (optional - at least one service required)
RADARR_URL=https://radarr.example.com
RADARR_API_KEY=example-api-key

# Sonarr Configuration (optional - at least one service required)
SONARR_URL=https://sonarr.example.com
SONARR_API_KEY=example-api-key

# TMDB Configuration (optional - improves media discovery)
TMDB_API_KEY=your-tmdb-api-key-here

# Tool Configuration (optional - defaults to "default" profile with discovery-add tools)
# Profile options: default, minimal, curator, maintainer, power-user, full
TOOL_PROFILE=default

# Additional tool branches to enable (comma-separated)
# Branches: library-management, system-maintenance, download-management, metadata-enrichment, advanced-search
# TOOL_BRANCHES=library-management,download-management

# Exclude specific tools (comma-separated)
# TOOL_EXCLUDE=radarr_delete_movie,sonarr_delete_series

# Include specific tools (comma-separated) - overrides profile/branch settings
# TOOL_INCLUDE=radarr_get_health,sonarr_get_health

# Path to JSON configuration file for advanced tool configuration
# TOOL_CONFIG_PATH=./tools.config.json

# Instructions:
# 1. Replace the URLs with your actual Radarr/Sonarr/TMDB instance URLs
# 2. Replace the API keys with your actual API keys
# 3. For TMDB functionality, get an API key from https://developer.themoviedb.org/
# 4. At least one service (Radarr, Sonarr, OR TMDB) must be configured
# 5. Radarr/Sonarr API keys can be found in Settings → General → Security in each application

# Example with different URLs:
# RADARR_URL=https://radarr.example.com
# SONARR_URL=https://sonarr.example.com

# Example with custom ports:
# RADARR_URL=http://192.168.1.100:7878
# SONARR_URL=http://192.168.1.100:8989